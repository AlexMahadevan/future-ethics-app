# Add this to your .gitignore to avoid committing sensitive credentials

# Airtable credentials
config.js
config.production.js

# Allow the root config.js so GitHub Pages can read Airtable creds. The PAT
# is scoped to one base and is exposed in browser network requests anyway.
# Rotate it after each workshop.
!/config.js
!archive-baltimore-2026/config.js

# System files
.DS_Store
.vscode/
*.log

# Dependencies (if you add any build tools later)
node_modules/
venv/
__pycache__/
*.pyc

# Staging copy of the LA app — the served version lives at the repo root
visual-standards/
